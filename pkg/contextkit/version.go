package contextkit

// APIVersion is the frozen public contract pin (ADR-0026).
const APIVersion = "v1"

// APIVersionHeader is the server response header for api_version.
const APIVersionHeader = "X-Context-API-Version"
